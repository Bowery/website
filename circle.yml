test:
  override:
    - echo "No Tests.."
deployment:
  production:
    branch: master
    commands:
      - npm install myth -g
      - myth static/bowery.css static/out.css
      - cd deploy && npm install
      - node deploy/index.js
