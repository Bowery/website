test:
  override:
    - echo "No Tests.."
deployment:
  production:
    branch: master
    commands:
      - npm install myth -g
      - myth static/bowery.css static/out.css
      - gem install jekyll
      - jekyll build
      - cp -rf _site/posts/* _site/blog/
      - cd deploy && npm install
      - node deploy/index.js
